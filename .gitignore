# Local environment files (never commit real secrets)
.env
*.env
# ...but keep committed example templates
!*.example.env

# limen CLI schema dump
.limen/

# Go coverage output (generated by auth-service/scripts/coverage.sh)
coverage.out
